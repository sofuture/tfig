X = done
* = todo

X calculate range -> pixels
    X create 2d array for screen 'pixels'
    X calculate width/height of pixel
    X run through data, put appropriate data in screen array

X draw array 'backwards' to avoid flipping

X uhm, actually work on linux
    X parse command line opts correctly
